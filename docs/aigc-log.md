# AIGC 使用记录

> 原则：每次实际协作后立即记录，不补造不存在的对话。至少完成 3 次代表性记录。

## 记录 1：作业要求分析与项目骨架

- 日期：2026-09-13
- 子任务：作业要求分析、评分点拆解、项目结构与开发路线设计
- 使用的 AIGC 技术：Codex
- 我提出的要求：分析第二次作业要求，并给出能够获得高分的实施步骤
- AI 实现或提供了什么：
  - 拆解游戏功能、GitHub、测试、AIGC 和博客要求
  - 按评分项目安排开发顺序
  - 建议将路径逻辑与 Pygame 界面分离，方便测试
  - 创建初始项目骨架、路径检测函数和逻辑测试
- 实际效果：路径逻辑自动测试 `8 passed`；SDL dummy 模式下的 Pygame 空窗口冒烟测试通过
- 人工修改/确认：
  - 用户提供了实际 GitHub 仓库地址
  - 创建 .venv 并安装 Pygame 2.6.1、pytest 8.4.2
  - 冒烟测试发现系统字体枚举异常，将 SysFont 改为 Pygame 自带字体后通过
- 对应文件：`main.py`、`game.py`、`logic.py`、`tests/test_logic.py`

## 后续记录模板

### 记录 N：标题

- 日期：
- 子任务：
- 使用的 AIGC 技术：
- 我提出的要求：
- AI 实现或提供了什么：
- 实际效果：
- 人工修改：
- 验证结果：

## 记录 2：开始界面与状态切换

- 日期：2026-09-13
- 子任务：设计并实现开始界面、开始按钮和界面状态切换
- 使用的 AIGC 技术：Codex
- 我提出的要求：先实现开始界面，并保留后续接入游戏棋盘的结构
- AI 实现或提供了什么：
  - 绘制标题、副标题、玩法说明和四个方向提示
  - 实现带悬停效果的“开始游戏”按钮
  - 增加 `START` 与 `PLAYING` 两个界面状态
  - 增加中文字体候选与默认字体回退逻辑
- 实际效果：自动测试 `8 passed`；SDL dummy 模式下开始界面绘制和按钮状态切换测试通过
- 人工修改：
  - 用户明确要求先只完成开始界面，不提前实现棋盘
  - 游戏界面暂用占位页验证状态切换，后续替换为真实棋盘
- 验证结果：代码和截图已生成，待用户在本机窗口中进行视觉确认
- 对应文件：`game.py`、`README.md`、`screenshots/start-screen-preview.png`

## 记录 3：游戏界面视觉重构与关卡数据

- 日期：2026-09-13
- 子任务：游戏 HUD、棋盘、箭头绘制、纸张风格视觉和关卡数据
- 使用的 AIGC 技术：Codex
- 我提出的要求：开始制作游戏界面，UI 不要有过重的 AI 模板感
- AI 实现或提供了什么：
  - 去掉蓝色渐变、厚阴影和悬浮胶囊按钮
  - 改为暖纸张、细线、墨色箭头和少量朱红点缀
  - 增加 HUD、棋盘、剩余箭头、剩余失误和重新开始按钮
  - 增加三组通过反向放置生成的可解关卡
  - 接入基础点击消除与阻挡失误反馈
- 实际效果：全部自动测试 `11 passed`；开始界面、游戏界面、点击消除和重新开始冒烟测试通过
- 人工修改：
  - 用户明确提出避免模板化 AI 视觉
  - 将界面中的英文装饰文案改为中文
  - 点击动画、通关流程和关卡切换留到后续阶段实现
- 验证结果：已生成开始界面和游戏界面预览图
- 对应文件：`game.py`、`ui.py`、`levels.py`、`tests/test_levels.py`

## 记录 4：游戏界面细节调整与一次性道具

- 日期：2026-09-13
- 子任务：根据用户反馈细化游戏界面，并加入三个一次性道具
- 使用的 AIGC 技术：Codex
- 我提出的要求：
  - 去掉底部“按 R 重新开始”
  - 游戏标题使用仓库英文名 `Arr0w-voyage`
  - 棋盘和格子使用轻微圆角与浅灰细边框
  - 箭头格悬浮时轻微高亮
  - 重新开始按钮使用圆角、细边框和悬停变色
  - 将“01 初试锋芒”替换为三个每关一次的道具
  - 将“剩余失误”改为“剩余失误次数”
- AI 实现或提供了什么：
  - 重构 `Button`，支持圆角、边框宽度和悬停配色
  - 棋盘改为圆角外框、圆角格子和浅灰分隔线
  - 增加“提示”“撤销”“移出”三个每关一次的道具
  - 提示会高亮一个可飞出箭头，撤销会恢复上一步，移出会移除当前指向的箭头
- 实际效果：自动测试 `11 passed`；界面、提示、撤销、移出和重新开始冒烟测试通过
- 人工修改：需求由用户逐项指定，AI 完成界面调整与道具交互接线
- 验证结果：已重新生成开始界面和游戏界面预览图
- 对应文件：`game.py`、`ui.py`、`README.md`

## 记录 5：道具图标与已使用状态

- 日期：2026-09-13
- 子任务：将文字道具改为图标加作用说明，并增加用后禁用状态
- 使用的 AIGC 技术：Codex
- 我提出的要求：每个道具显示图标和作用，不显示剩余次数；用完后直接变灰或变暗
- AI 实现或提供了什么：
  - 提示、撤销、移出分别使用灯泡、回转箭头、箭头移出方框图标
  - 每个道具显示名称和作用说明
  - 使用成功后将整个道具切换为灰色并显示“已使用”
  - 已使用的道具不再响应鼠标悬停和点击
- 实际效果：自动测试 `11 passed`；图标道具和已使用状态冒烟测试通过
- 验证结果：新增 `screenshots/tool-used-preview.png`
- 对应文件：`ui.py`、`game.py`、`screenshots/game-screen-preview.png`、`screenshots/tool-used-preview.png`

## 记录 6：将撤销道具替换为增加失误次数

- 日期：2026-09-13
- 子任务：移除撤销道具，替换为可增加一次剩余失误次数的道具
- 使用的 AIGC 技术：Codex
- 我提出的要求：撤销道具没什么用，改成增加一次失误次数
- AI 实现或提供了什么：
  - 移除撤销方法和历史步骤记录
  - 将中间道具改为“增加失误次数”和加号图标
  - 使用后默认失误上限从 3 增加到 4
  - 道具使用后立即变灰并显示“已使用”
- 发现与修复：测试时发现重新开始没有恢复默认失误上限，已在关卡加载逻辑中修复
- 实际效果：自动测试 `11 passed`；增加失误次数、重新开始重置、提示和移出道具冒烟测试通过
- 验证结果：已重新生成游戏界面和道具已使用状态预览图
- 对应文件：`game.py`、`ui.py`、`README.md`

## 记录 7：界面收尾调整与移出道具修复

- 日期：2026-09-14
- 子任务：根据反馈调整开始界面、顶部按钮、道具余量和背景颜色，并修复移出道具
- 使用的 AIGC 技术：Codex
- 我提出的要求：
  - 开始界面删除“鼠标点击开始 / Esc 退出”
  - 游戏界面删除底部 Esc 提示，重新开始按钮左移，右侧增加退出游戏按钮
  - “每关一次 / 道具”改为“道具”，最右侧用 X1 表示剩余次数
  - 背景改为白色
  - “移出”道具要能够正常使用
  - 开始界面主标题改为 Arr0w-voyage，“一箭又一箭”作为更大的副标题
- AI 实现或提供了什么：
  - 将移出道具改为“先点道具，再点箭头”的两步操作，避免丢失鼠标悬停目标
  - 增加右上角退出游戏按钮
  - 道具剩余次数改为 X1，使用后变灰并显示 X0
  - 主背景改为白色，保留极轻的纸面颗粒
  - 开始界面主标题上移，中文副标题使用更大的粗体字
- 实际效果：自动测试 `11 passed`；移出两步操作、退出按钮、白色背景和标题布局冒烟测试通过
- 验证结果：已重新生成三张界面预览图
- 对应文件：`game.py`、`ui.py`、`README.md`、`docs/test-report.md`

## 记录 8：红心失误显示与满血判断

- 日期：2026-09-14
- 子任务：将剩余失误次数改为红心，并把增加失误次数改为回血道具
- 使用的 AIGC 技术：Codex
- 我提出的要求：
  - 剩余失误显示为三颗红心，失误后对应红心变灰
  - 增加失误次数只在掉血后可以使用
  - 满血时使用需要弹出“满血不可用”的提示，且不能消耗道具
- AI 实现或提供了什么：
  - 增加矢量红心绘制函数，默认显示三颗红心
  - 每次失误后按剩余数量将红心变灰
  - 增加失误次数改为恢复一颗红心，不再提高上限
  - 满血使用时显示顶部提示，道具保持 X1
  - 掉血后使用成功，红心恢复且道具变灰显示 X0
- 实际效果：自动测试 `11 passed`；满血禁用、红心变灰、回血和道具消耗冒烟测试通过
- 验证结果：生成满血提示和掉血红心预览图
- 对应文件：`game.py`、`ui.py`、`README.md`、`docs/test-report.md`

## 记录 9：代码模块化拆分

- 日期：2026-09-14
- 子任务：拆出道具模块、动画模块和音乐音效接口
- 使用的 AIGC 技术：Codex
- 我提出的要求：把代码进一步拆分，方便后续增加音乐等模块
- AI 实现或提供了什么：
  - 新增 `tools.py`，统一管理道具定义、一次性次数和目标选择状态
  - 新增 `animations.py`，统一管理碰撞反馈、提示和 Toast 计时
  - 新增 `audio.py`，预留音效文件路径和安全播放接口
  - `game.py` 保留流程编排与界面绘制，通过模块实例调用状态
  - 新增 `tests/test_tools.py` 和 `tests/test_animations.py`
- 实际效果：自动测试从 11 项增加到 18 项，全部通过；完整界面冒烟测试通过
- 人工修改：模块拆分后按原界面行为逐项回归，确认提示、回血、移出和退出功能正常
- 对应文件：`tools.py`、`animations.py`、`audio.py`、`game.py`、`tests/`

## 记录 10：统一棋盘风格与按钮素材

- 日期：2026-09-15
- 子任务：让开始界面棋盘使用游戏界面的素材风格，并统一开始、游戏、结算按钮
- 使用的 AIGC 技术：Codex
- 我提出的要求：
  - 开始界面的棋盘改成游戏界面的风格
  - 开始界面、游戏界面和结算界面的按钮使用 `assets/ui/按钮.png`
- AI 实现或提供了什么：
  - 为 `按钮.png` 增加九宫格缩放函数，避免直接拉伸导致圆角和边框变形
  - `Button` 组件统一改用 `assets/ui/按钮.png`
  - 悬停时只对按钮的可见像素染成对应颜色，保留透明圆角
  - 开始界面棋盘改用 `方格.png` 和与游戏界面相同的边框、阴影及箭头素材
  - 新增按钮九宫格与悬停染色测试
- 实际效果：自动测试 `60 passed`；开始、游戏、关卡完成、全部通关和失败界面均可正常绘制
- 验证结果：重新生成开始、游戏和三张结算界面截图
- 对应文件：`ui.py`、`game.py`、`tests/test_ui_assets.py`
